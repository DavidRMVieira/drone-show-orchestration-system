# Customer App Server configuration
# Use 'localhost' for local development, or the DEI server for production
# Customer App configuration
# Local environment (default):
server.host=localhost
server.port=8080
# DEI environment (uncomment the lines below):
#server.host=vs791.dei.isep.ipp.pt
#server.port=8080
